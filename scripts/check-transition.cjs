// Run with Playwright on NODE_PATH and CHECK_URL pointing to the built preview.
const {chromium}=require('playwright');const sharp=require('sharp');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true,args:['--enable-webgl','--use-angle=swiftshader','--enable-unsafe-swiftshader']});try{const p=await b.newPage({viewport:{width:1280,height:800}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{window.earthTools={};Object.defineProperty(document,'modelContext',{value:{registerTool:t=>{window.earthTools[t.name]=t;}}});});await p.goto(process.env.CHECK_URL||'http://localhost:3001/');await p.waitForFunction(()=>window.earthTools.explore_earth_time);await p.getByRole('button',{name:'Stop globe rotation'}).click();let prev;for(const age of [620,590,560,540.01,539.99,530,510]){await p.evaluate(age=>window.earthTools.explore_earth_time.execute({millionYearsAgo:age}),age);await p.waitForTimeout(350);const png=await p.locator('canvas').first().screenshot();const {data}=await sharp(png).resize(160,100).removeAlpha().raw().toBuffer({resolveWithObject:true});if(prev){let delta=0;for(let i=0;i<data.length;i++)delta+=Math.abs(data[i]-prev.data[i]);delta/=data.length;console.log(prev.age+' → '+age+': mean pixel change '+delta.toFixed(3));if(age===539.99)assert(delta<2,'Discontinuous 540 Ma boundary');}prev={age,data};await p.screenshot({path:'outputs/transition-'+age+'.png'});}await p.getByRole('link',{name:'Human Odyssey',exact:true}).click();await p.waitForURL('**/human-odyssey');await p.getByRole('heading',{name:'Human Odyssey.'}).waitFor();await p.waitForSelector('canvas');await p.waitForFunction(()=>!document.querySelector('.globe-loading'));await p.getByRole('button',{name:'Play Human Odyssey demo',exact:true}).click();await p.waitForFunction(()=>Number(document.querySelector('input[aria-label="Human chapter progress"]').value)>0);assert.deepEqual(errors,[]);console.log('PASS navigation, globe load, and Human Odyssey playback');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});

